# Sugestões de Comandos
Estes são os comandos fundamentais para interagir com o projeto e o assistente de IA:

## IA Dev Superpowers
- `aidev status`: Verifica o estado atual do projeto, stack e sessão.
- `aidev agent`: Gera o prompt de ativação para o modo agente.
- `aidev init`: Executa a instalação inicial do sistema/blueprint.
- `aidev snapshot`: Gera um resumo técnico (context passport).
- `aidev upgrade`: Atualiza o orquestrador e agentes.

## Desenvolvimento Laravel (via Sail/Docker)
- `sail artisan migrate`: Executa as migrações.
- `sail npm run dev`: Inicia o servidor de assets.
- `sail artisan test`: Executa a suite de testes (importante para TDD).
